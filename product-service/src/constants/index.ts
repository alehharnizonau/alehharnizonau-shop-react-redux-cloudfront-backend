export enum Status {
  Success = 200,
  Error = 404,
  ServerError = 500
}

export enum ErrorMessage {
  NotFound = 'not found',
  ServerError = 'Internal server error'
}

export const pathApi = 'qq1272e6fd.execute-api.us-east-1.amazonaws.com'